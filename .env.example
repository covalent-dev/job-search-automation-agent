# Proxy (Residential / ISP / Datacenter)
PROXY_HOST=geo.iproyal.com
PROXY_PORT=12321
PROXY_USERNAME=your_username
PROXY_PASSWORD=your_password

# Optional convenience vars (not required by current YAML configs)
# PROXY_ENABLED=true
# PROXY_TYPE=http
# PROXY_URL=http://user:pass@host:port

# Proxy pool (local-only usage by scripts; do not commit real values)
# Format: comma-separated `host:port:username:password` entries.
# Used by: `python3 scripts/proxy_pool_smoke.py` and `python3 scripts/proxy_pool_reliability.py`
ISP_PROXY_ENDPOINTS=host:port:username:password,host:port:username:password

# Captcha Providers
# 2captcha
CAPTCHA_API_KEY=your_2captcha_api_key

# CapSolver (recommended for Cloudflare Turnstile)
CAPSOLVER_API_KEY=your_capsolver_api_key
